{
  "_comment": "DOGFOOD version — points the marketplace at the local agent-tooling checkout instead of GitHub, so you can prove the plugin before publishing. Put this at <repo>/.claude/settings.local.json (gitignored, personal) OR just run `/plugin marketplace add /absolute/path/to/agent-tooling` once interactively. Replace the path below with wherever YOU checked out agent-tooling.",
  "extraKnownMarketplaces": {
    "agent-tooling-local": {
      "source": { "source": "directory", "path": "/absolute/path/to/agent-tooling" }
    }
  },
  "enabledPlugins": { "ceremony@agent-tooling-local": true }
}
