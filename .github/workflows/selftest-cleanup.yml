name: selftest cleanup reusable
on:
  workflow_dispatch: {}
permissions:
  pull-requests: write
  contents: read
jobs:
  call:
    uses: ./.github/workflows/_probe.yml
    with:
      stale_hours: 24
